import styled from 'styled-components';

const StyledImage = styled.img`
    max-width: 100%;
    max-height: 100%;
    width: auto;
    height: auto;
    object-fit: contain;
`;

export default StyledImage;
